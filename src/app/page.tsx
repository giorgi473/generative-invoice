import { Button } from "@/components/ui/button";

function HomePage() {
  return (
    <div>
      <Button variant={"ghost"} size={"default"} className="ml-3 mt-3">
        add button
      </Button>
    </div>
  );
}

export default HomePage;
